# JEED 阶段二：服务器部署判断

结论：`client_only`，依据第 0 阶段高置信度初判快速通过。

- 客户端入口只注册屏幕事件和 JEI/REI/EMI 展示内容。
- 没有物品、方块、实体、配方、数据包、网络协议、存档或世界生成内容。
- JAR 内的两个配方类型仅作为 JEI/REI/EMI 效果页面的数据结构使用，不改变服务端配方。

不复制到服务器暂存目录、服务器 `mods` 目录或服务器清单。联机客户端保留 JEED 即可。
