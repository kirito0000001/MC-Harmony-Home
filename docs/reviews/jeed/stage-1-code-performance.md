# JEED 阶段一：代码与性能审查

结论：未发现有实际收益且低风险的源码优化点，不改动运行逻辑。

- `getEffectList()` 在 JEI/REI/EMI 注册期生成效果列表，规模有限，不是每帧或每 tick 热路径。
- 客户端运行时只在打开相关屏幕后处理当前鼠标位置的效果悬浮提示和点击，不创建网络任务、线程池或异步任务。
- 未发现未关闭流、重复 IO、网络访问或服务端路径。

保留上游逻辑，避免为了极小的启动期分配减少而增加缓存失效或兼容风险。
